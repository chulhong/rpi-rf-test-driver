# === Edit these to your setup ===
COUNTRY=FI

# The Mac's IP address on the lab Wi-Fi (used when Pi acts as client)
MAC_IP=10.0.2.201

# iperf3 UDP target bitrates (tune to saturate without massive loss)
UDP_MBPS_24=100
UDP_MBPS_5=400

# Optional fixed Wi-Fi TX power (mBm). Comment to let driver decide.
# FORCE_TXPOWER_MBM=2000   # 20 dBm
